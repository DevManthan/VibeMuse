/**
 * VibeMuse — Agent Module (Step 3)
 * Connects to the Gemini API, routes tool calls, manages history.
 * Placeholder — full implementation in Step 3.
 */

export function initAgent() {
  console.log('[Agent] Agent module placeholder initialized.');
  // Full Gemini API + tool routing in Step 3
}
